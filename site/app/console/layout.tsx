import './console.css';

export const metadata = {
  title: 'SESDB Console · Interactive Demo',
  description: 'Explore SESDB with safe, interactive sample session data.',
};

export default function ConsoleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
